# Hoist Prisma packages so the `prisma` CLI can resolve `@prisma/client`
# regardless of which workspace cwd it runs from. Without this, pnpm's
# strict layout breaks `prisma generate` lifecycle scripts.
public-hoist-pattern[]=*prisma*
public-hoist-pattern[]=@prisma/*
