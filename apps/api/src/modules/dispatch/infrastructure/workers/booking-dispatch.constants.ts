export const BOOKING_DISPATCH_QUEUE_NAME = "booking-dispatch";
export const BOOKING_DISPATCH_JOB = "process-pending";
/** Worker tick — every 30 s by default; configurable via env. */
export const DEFAULT_BOOKING_DISPATCH_INTERVAL_MS = 30_000;
