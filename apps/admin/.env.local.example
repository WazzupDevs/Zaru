# Copy to .env.local for development.
# Points the admin browser at the local API. Production deploy injects
# the public API URL via the host (Coolify env binding, A4).
NEXT_PUBLIC_API_URL=http://localhost:3000
